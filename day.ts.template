import { transforms } from 'advent-of-code-client';

export function part1(input: transformType): number | string {
    return 0;
}

export function part2(input: transformType): number | string {
    return 0;
}

type transformType = string[];
const transform = transforms.lines;
// const transform = (d: string): transformType => d;

const testData = {
    part1: [``],
    part2: [``]
};

const testAnswers = {
    part1: [0],
    part2: [0]
};

export { transform, testData, testAnswers };